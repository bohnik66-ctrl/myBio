<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bio Page</title>
    <style>
        /* Основной фон - темно-синий градиент */
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: radial-gradient(circle at center, #1a2a6c, #0b1026, #000000);
            font-family: 'Inter', -apple-system, sans-serif;
            color: white;
        }

        /* Стеклянная карточка */
        .glass-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 40px;
            padding: 50px 30px;
            text-align: center;
            width: 90%;
            max-width: 380px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
        }

        /* Контейнер для аватарки с крестом */
        .avatar-container {
            width: 100px;
            height: 100px;
            background: white;
            border-radius: 50%;
            margin: 0 auto 25px;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0 0 20px rgba(56, 189, 248, 0.4);
            position: relative;
        }

        /* Рисуем черный христианский крест через CSS */
        .cross {
            background: black;
            position: absolute;
        }
        .cross-v { width: 8px; height: 50px; } /* Вертикальная перекладина */
        .cross-h { width: 30px; height: 8px; top: 30px; } /* Горизонтальная перекладина */

        h1 {
            font-size: 28px;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 700;
        }

        .bio-text {
            font-size: 15px;
            color: #cbd5e1;
            line-height: 1.6;
            margin-bottom: 35px;
        }

        /* Кнопки как на картинке */
        .btn-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .btn {
            background: linear-gradient(180deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: white;
            padding: 16px;
            border-radius: 15px;
            text-decoration: none;
            font-weight: 500;
            font-size: 16px;
            transition: 0.3s all ease;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }

        .btn:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
            border-color: #38bdf8;
        }
    </style>
</head>
<body>

    <div class="glass-card">
        <div class="avatar-container">
            <div class="cross cross-v"></div>
            <div class="cross cross-h"></div>
        </div>
        
        <h1>Raffik</h1>
        
        <p class="bio-text">
            Дизайнер / Разработчик / Игроман.<br>
            Занимаюсь разработкой ботов в телеграме.
            Разработкой приложений и игр.
        </p>

        <div class="btn-container">
            <a href="https://t.me/Raffidzo" class="btn">Telegram Bio</a>
            <a href="https://t.me/Rafikprojects" class="btn">Мои работы</a>
            <a href="https://t.me/Rraaffidzo" class="btn">Личные сообщения</a>
        </div>
    </div>

</body>
</html>
